# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
dist-ssr
out
*.local
tsconfig.tsbuildinfo

# server runtime data (waitlist signups, etc.)
server/data

# env
.env
.env.*
!.env.example

# editor / OS
.vscode
.idea
.DS_Store
*.log
*.sw?

# capacitor — iOS native project is regenerated; the Android project is committed
# (Capacitor writes its own android/.gitignore for build outputs).
/ios
